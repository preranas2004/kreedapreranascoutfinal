rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic Auth Check for Debugging
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    // Connection Test
    match /_connection_test_/{docId} {
      allow read: if true;
    }
  }
}
